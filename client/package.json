{
  "name": "bip-explorer-client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd .. && npx vite build --root client --outDir dist",
    "dev": "cd .. && npx vite client",
    "preview": "cd .. && npx vite preview"
  }
}